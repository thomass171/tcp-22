package de.yard.threed.core.platform;

/**
 * Runs a sequence of runnables, depending on platform sequentially or pseudo sequentially via callback.
 * Just a placeholder for a comment. Hard to implement.
 */
/*public interface NativeAsyncRunner {

    /**
     * Schedule a runnable to be executed later in the 'same thread'. So, explicitly no concurrent execution
     * to the main thread (the thread that renders).
     * Like SwingUtilities.invokeLater or  AnimationScheduler.requestAnimationFrame();
     * Executed by platform natively, not by AsyncHelper.
     * /
    void invokeLater(NativeRunnable runnable, int delay);
}*/
