using System;
//using System.Drawing; gibts nicht in unity!
using System.IO;
using UnityEngine;
using java.lang;
using java.util;
using de.yard.threed.core.platform;


namespace de.yard.threed.platform.unity
{
    /**
     */
    public class UnityImageUtil
    {
        static Log logger = Platform.getInstance ().getLog (typeof(UnityImageUtil));

    
       /*19.4.16  public static byte[] loadImageFromFile (string filePath)
        {
            byte[] data = null;
           //FileReader prueft existenz
                data = UnityFileReader.getFileStream (filePath).readFully ();
                
                //TODO error handling
            
            //logger.debug ("texture loaded tex=" + tex);
            return data;
        }*/
    }
}