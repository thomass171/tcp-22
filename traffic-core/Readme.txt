not dependent from engine! Should be usable from WebServices, but needs to be C# and GWT ready (using StringUtils)

29.3.25 SmartLocation no longer here but in traffic? But even though it is just a convention, it might be
useful in traffic-core for validations in a web service.