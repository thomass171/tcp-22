package de.yard.threed.engine;


import de.yard.threed.core.Util;

/**
 * Created by thomass on 25.04.15.
 */
public class TubeGeometry extends Geometry {

    public TubeGeometry(Curve path, int segments, float radius, int radialSegments, boolean closed, boolean debug){
        Util.notyet();
        //geometry = WebGlGeometry.buildTubeGeometry(path.curve, segments, radius, radialSegments, closed, debug);
    }

}
